services:
  - type: web
    name: baby-and-me-backend
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
